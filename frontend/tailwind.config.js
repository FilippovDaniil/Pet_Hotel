/** @type {import('tailwindcss').Config} */
// Конфигурация Tailwind CSS — управляет какие утилиты генерировать и как расширить дизайн-систему.
export default {
  // content: список файлов для сканирования на использование Tailwind-классов.
  // Tailwind анализирует эти файлы и включает в бандл только используемые классы (tree-shaking).
  // index.html включён для классов в шаблоне, src/**/*.{js,ts,jsx,tsx} — для компонентов.
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  theme: {
    extend: {
      // colors.primary: кастомная цветовая палитра для всего проекта.
      // Расширяем (extend), а не заменяем — стандартные цвета Tailwind (gray, red, green...) сохраняются.
      // Оттенки 50–900 от светлого к тёмному — это цветовая шкала Tailwind по умолчанию.
      // Значения соответствуют синей палитре (#eff6ff–#1e3a8a), близкой к blue-* из Tailwind v3.
      colors: {
        primary: {
          50:  '#eff6ff',  // очень светлый (фон hover-состояний, bg-primary-50)
          100: '#dbeafe',
          200: '#bfdbfe',
          300: '#93c5fd',
          400: '#60a5fa',
          500: '#3b82f6',  // средний (focus:ring-primary-500)
          600: '#2563eb',  // основной (bg-primary-600 — кнопки, активные элементы)
          700: '#1d4ed8',  // тёмный (hover:bg-primary-700)
          800: '#1e40af',
          900: '#1e3a8a',  // самый тёмный (редко используется)
        },
      },
    },
  },
  plugins: [],  // дополнительные Tailwind-плагины не подключены
}
